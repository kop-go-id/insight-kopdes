# OpenAI / LLM
OPENAI_API_KEY="sk-proj-xxx"

# Postgres (SQLAlchemy URI). Use read-only user.
DATABASE_URL=postgresql+psycopg2://kopdes_merah_putih:R4h%40s1a..@34.101.102.141:5432/kopdes_merah_putih

EXCLUDE_TABLES=cooperative_history,delete_logs,cooperative_member_account_banks

# Max rows to fetch (safety)
MAX_ROWS=9999999

# How many sample rows to include in the prompt for each table (0 to disable)
SAMPLE_ROWS=2

# Optional: port for FastAPI
PORT=8000